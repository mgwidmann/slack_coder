fragment PR on PullRequest {
  id
  analysisStatus
  analysisUrl
  backoff
  baseBranch
  branch
  number
  buildStatus
  buildUrl
  htmlUrl
  owner
  repository
  closedAt
  mergedAt
  title
  user {
    id
    avatarUrl
    name
  }
}
